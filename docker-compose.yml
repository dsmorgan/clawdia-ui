services:
  clawdia-ui:
    build: .
    container_name: clawdia-ui
    restart: unless-stopped
    ports:
      - "${PORT:-8013}:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    env_file:
      - .env
